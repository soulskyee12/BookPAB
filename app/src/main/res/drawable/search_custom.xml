<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/abu_abu" /> <!-- Background color -->
    <corners android:radius="20dp" /> <!-- Adjust the radius for the curve -->
    <padding
        android:left="8dp"
        android:top="8dp"
        android:right="8dp"
        android:bottom="8dp" />
</shape>
